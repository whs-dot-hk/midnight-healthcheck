[package]
name = "healthcheck"
version = "1.2.0"
edition = "2021"
description = "JSON-RPC 2.0 healthcheck over stdio: host + Midnight, cardano-node, db-sync"

[[bin]]
name = "healthcheck"
path = "src/main.rs"

[dependencies]
libc = "0.2"
serde_json = "1"
